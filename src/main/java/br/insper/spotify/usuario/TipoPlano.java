package br.insper.spotify.usuario;

public enum TipoPlano {
    // Enumerations = classe especial que representa um grupo de constantes. Quando usamos ela como atributo em um objeto, ela pode ser apenas um dos itens listados.
    FREE,
    PREMIUM
}
